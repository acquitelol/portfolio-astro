import React from 'react'
import './footer.css'

interface containerProps {
    
}

const Navbar: React.FC<containerProps> = ({}) => {
    return (
        <div className='footer'>Made by Acquite.</div>
    );
};
  

export default Navbar;